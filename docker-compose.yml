version: '3.8'
services:
  kaspa-ip-monitor:
    build: .
    ports:
      - "8080:8080"
    environment:
      - API_URL=https://api.runonflux.io/apps/location/KaspaNodeIgra
    volumes:
      - ./data:/data
